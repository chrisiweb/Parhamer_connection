\section{PWLU - 2 - K6 - Rechenregeln Potenzen - ZO - MatKon}

\begin{beispiel}[PWLU]{1}
Ordne jedem Term in der linken Tabelle das zugehörige Ergebnis aus der rechten Tabelle zu.\vspace{0,2cm}

\zuordnen{
				R1={$(a^x)^y$},				% Response 1
				R2={$a^x\cdot a^y$},				% Response 2
				R3={$a^\frac{x}{y}$},				% Response 3
				R4={$a^{-x}\cdot y$},				% Response 4
				%% Moegliche Zuordnungen: %%
				A={$\sqrt[y]{a^x}$}, 				%Moeglichkeit A  
				B={$a^{x+y}$}, 				%Moeglichkeit B  
				C={$\dfrac{y}{a^x}$}, 				%Moeglichkeit C  
				D={$a^{x\cdot y}$}, 				%Moeglichkeit D  
				E={$a^x+a^y$}, 				%Moeglichkeit E  
				F={$a^{x-y}$}, 				%Moeglichkeit F  
				%% LOESUNG: %%
				A1={D},				% 1. richtige Zuordnung
				A2={B},				% 2. richtige Zuordnung
				A3={A},				% 3. richtige Zuordnung
				A4={C},				% 4. richtige Zuordnung
				}
\end{beispiel}
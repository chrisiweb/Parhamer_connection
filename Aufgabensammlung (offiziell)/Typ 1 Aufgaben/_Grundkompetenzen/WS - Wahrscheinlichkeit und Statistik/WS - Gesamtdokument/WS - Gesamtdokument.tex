 \documentclass[a4paper,12pt]{report}

\usepackage{geometry}
\geometry{a4paper,left=18mm,right=18mm, top=3cm, bottom=2cm} 


\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage{eurosym}
\usepackage{setspace}
\usepackage[latin1]{inputenc}
\usepackage{graphicx}
\usepackage[ngerman]{babel}
\usepackage{mathematik}
%\usepackage{ziffer}
%\usepackage{pst-eucl}


\pagestyle{plain} %PAGESTYLE: empty, plain, fancy
\onehalfspacing %Zeilenabstand
\setcounter{secnumdepth}{-1} % keine Nummerierung der Überschriften
\setcounter{Antworten}{1}

%
%
%
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% DOKUMENT - ANFANG 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
%
%
%
\begin{document}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 1.1/Einzelbeispiele/WS 1.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}


\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 1.2/Einzelbeispiele/WS 1.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 1.3/Einzelbeispiele/WS 1.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 1.4/Einzelbeispiele/WS 1.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 2.1/Einzelbeispiele/WS 2.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 2.2/Einzelbeispiele/WS 2.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}


\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 2.3/Einzelbeispiele/WS 2.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 2.4/Einzelbeispiele/WS 2.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 3.1/Einzelbeispiele/WS 3.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 3.2/Einzelbeispiele/WS 3.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 3.3/Einzelbeispiele/WS 3.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 3.4/Einzelbeispiele/WS 3.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../WS 4.1/Einzelbeispiele/WS 4.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}
\end{document}


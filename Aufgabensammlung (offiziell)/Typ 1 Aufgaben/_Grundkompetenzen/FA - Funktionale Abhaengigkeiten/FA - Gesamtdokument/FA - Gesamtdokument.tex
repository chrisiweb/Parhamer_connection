 \documentclass[a4paper,12pt]{report}

\usepackage{geometry}
\geometry{a4paper,left=18mm,right=18mm, top=3cm, bottom=2cm} 


\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage{eurosym}
\usepackage{setspace}
\usepackage[latin1]{inputenc}
\usepackage{graphicx}
\usepackage[ngerman]{babel}
\usepackage{mathematik}
%\usepackage{ziffer}
%\usepackage{pst-eucl}


\pagestyle{plain} %PAGESTYLE: empty, plain, fancy
\onehalfspacing %Zeilenabstand
\setcounter{secnumdepth}{-1} % keine Nummerierung der Überschriften
\setcounter{Antworten}{1}

%
%
%
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% DOKUMENT - ANFANG 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
%
%
%
\begin{document}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.1/Einzelbeispiele/FA 1.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.2/Einzelbeispiele/FA 1.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}


\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.3/Einzelbeispiele/FA 1.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.4/Einzelbeispiele/FA 1.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.5/Einzelbeispiele/FA 1.5 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.6/Einzelbeispiele/FA 1.6 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.7/Einzelbeispiele/FA 1.7 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.8/Einzelbeispiele/FA 1.8 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 1.9/Einzelbeispiele/FA 1.9 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 2.1/Einzelbeispiele/FA 2.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 2.2/Einzelbeispiele/FA 2.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 2.3/Einzelbeispiele/FA 2.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 2.4/Einzelbeispiele/FA 2.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 2.5/Einzelbeispiele/FA 2.5 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 2.6/Einzelbeispiele/FA 2.6 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 3.1/Einzelbeispiele/FA 3.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 3.2/Einzelbeispiele/FA 3.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 3.3/Einzelbeispiele/FA 3.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 3.4/Einzelbeispiele/FA 3.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 4.1/Einzelbeispiele/FA 4.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 4.2/Einzelbeispiele/FA 4.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 4.3/Einzelbeispiele/FA 4.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 4.4/Einzelbeispiele/FA 4.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 5.1/Einzelbeispiele/FA 5.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 5.2/Einzelbeispiele/FA 5.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 5.3/Einzelbeispiele/FA 5.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 5.4/Einzelbeispiele/FA 5.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 5.5/Einzelbeispiele/FA 5.5 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 5.6/Einzelbeispiele/FA 5.6 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 6.1/Einzelbeispiele/FA 6.1 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}


\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 6.2/Einzelbeispiele/FA 6.2 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 6.3/Einzelbeispiele/FA 6.3 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}


\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 6.4/Einzelbeispiele/FA 6.4 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 6.5/Einzelbeispiele/FA 6.5 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}

\foreach \i in {1,...,2000} {%
    \edef\FileName{"../FA 6.6/Einzelbeispiele/FA 6.6 - "\i}%     The % here are necessary to eliminate any
    \IfFileExists{\FileName}{%  spurious spaces that may get inserted
       \input{\FileName}%       at these points

\hrule  \leer
 
		}
}
\end{document}

